includes:
    - ./vendor/phpstan/phpstan-phpunit/extension.neon

parameters:
    bootstrapFiles:
        - ./tests/bootstrap.php

    level: max

    reportUnmatchedIgnoredErrors: true

    excludePaths:
        - ./prod/php/vendor_per_PHP_version/*
        - ./tests/substitutes/*/original/*
