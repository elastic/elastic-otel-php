# EDOT PHP version (distinct from upstream version)
version=1.4.0
upstream_version=0.2.0

# All build parameters (supported_php_versions, otel_proto_version, etc.)
# are read from upstream/project.properties — do NOT duplicate them here.
